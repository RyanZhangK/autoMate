# Use pyproject.toml as the source of truth.
#
#   pip install -e .                # core (server + storage)
#   pip install -e '.[full]'        # core + mcp + browser + desktop
#
# Or install the published package:
#
#   pip install 'automate-hub[full]'
#
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
cryptography>=41.0
